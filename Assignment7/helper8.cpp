#include"Header.h"
void Display(char ch)
{
		
	 if((ch >= 'A') && (ch <= 'Z'))
	{
		printf("it is small\n");
	}
	 if ((ch >= 'a') && (ch <= 'z'))
	 {
		 printf("It is small");
	 }
	 else
	 {
		 printf("It is Digit\n");
	 }
	 

	 
	
}