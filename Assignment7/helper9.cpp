#include"Header.h"
UINT SumRange(int istart, int iend)
{
	int icnt = 1, isum = 0, i = 0, itemp = 0;
	if (istart > iend || iend == 0)
	{
		return 1;
	}
	if (istart < 0)
	{
		istart = -istart;
	}
	else
	{
		printf("It is digit");
	}
}